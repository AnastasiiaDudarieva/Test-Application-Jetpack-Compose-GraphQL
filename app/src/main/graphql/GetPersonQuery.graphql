query GetPerson($id: ID) {
    person(id: $id) {
        id
        name
        homeworld {
            name
        }
        height
        hairColor
        gender
        eyeColor
        skinColor
        mass
    }
}